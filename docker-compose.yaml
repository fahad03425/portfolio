version: "3.8"

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=False
      - ALLOWED_HOSTS=3.142.130.57,your-domain.com,www.your-domain.com,
